# 📁 โฟลเดอร์สำหรับไฟล์ 3D Model

## วางไฟล์ lkn.glb ที่นี่

```
public/models/
└── lkn.glb    👈 วางไฟล์ตรงนี้
```

### ขั้นตอน:
1. หาไฟล์ `lkn.glb` ของคุณ
2. Copy ไฟล์มาวางในโฟลเดอร์นี้
3. Refresh หน้าเว็บ (หรือ Vite จะ hot reload อัตโนมัติ)

### หมายเหตุ:
- ไฟล์จะต้องชื่อ `lkn.glb` ตรงตามที่กำหนดใน `GLTFHumanModel.jsx`
- ถ้าต้องการเปลี่ยนชื่อ แก้ไขใน `/src/components/GLTFHumanModel.jsx` บรรทัด `useGLTF('/models/lkn.glb')`
- ขนาดไฟล์แนะนำไม่เกิน 50 MB
- รองรับไฟล์ `.glb` และ `.gltf`
